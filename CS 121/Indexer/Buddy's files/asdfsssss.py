import os
path = r'C:\Users\Buddy\PycharmProjects\CS121Indexer'
for each result up to 5
    file_name = str(ordered_List[result]) + '.txt'

    data_file = open(os.path.join(path, file_name),'r')
    file = json.loads(data_file.read())

    #store file[text].split() into a list

    for word in (list of body words):
        if word == search term:
            #store first 15 and previous 5 w/ try except to object
            #break


